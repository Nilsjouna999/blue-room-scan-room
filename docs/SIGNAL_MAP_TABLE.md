# BLUE ROOM — Signal Map (altitude table)

Layer: ROUTER — **pointers + status only, never canon** · **SEEDED / INITIAL set, NOT complete** · 2026-06-17

One row per named Blue Room item, placed by its **designed** scope via the scope-span
removal-test. The placement rule lives in `docs/GOVERNANCE_OS.md` (it sources this table's
altitude spine); this file holds only **pointers + status** — every cell links back to the
authoritative source. The six-column set is **closed**: never add a column or a notes/rationale
cell (that is the whole anti-bloat defense). Source files stay authoritative; the table is the
stale pointer.

**This is a seeded first batch** — the items the Gate-3 proof already validated, placed correctly.
It is deliberately partial; it does **not** claim to place every item in the repo.

**Worked example (placement, cited not canon).** The room-as-place floor-wash fork
(`UNIVERSE_ZONE_MAP_V1` §3): "one static floor tone per inhabited zone, never an accent wash."
Designed scope = each inhabited zone's place-feeling. Removal cascade: **L1?** no (no product
law/safety/versioning breaks). **L2?** no (Read-Line, Card System, Halo Gate all still function).
**L3?** yes — every zone loses its place-feeling. It is *expressed* as one low-α CSS wash (an
L4-looking technique), so the blast-radius spans L3–L4 → the **tie-rule** takes the **higher** band
because the designed scope is the zone's place-feeling, the CSS being only its expression.
**→ altitude L3-Surface-Spec.**

**Reading the columns.** `altitude` = the band the removal-test resolves to. `status` shows each
item's **own** source token (no normalization): `LOCKED`/`ACTIVE`/`DRAFT`/`RETIRED`, plus `CONTEST`
(a live per-decision contest), `HOLDING` (in Backlog awaiting a decision), and `LOCKED-STATE-A/-B`
(a LOCKED-grade rule enforced by absence today (A), flipping in place when the engine ships (B)).
`holding-age` is in **BR-S sessions**; a row force-escalates at **4** via the next session's
pre-edit critique. `(LATER)` in a label = a committed-but-deferred build beat.

| altitude | status | label | rank/winner | link | holding-age |
| --- | --- | --- | --- | --- | --- |
| META | ACTIVE | This map + its altitude rule — placed once here, never altitude-placed | — | `docs/GOVERNANCE_OS.md` (Signal Map rule) | — |
| L1 Project-Law | LOCKED | No-face-rating law [GOLDEN_NUGGETS #1] | — | `docs/GOLDEN_NUGGETS.md` · `docs/DECISION_LOG.md` (2026-06-12) | — |
| L1 Project-Law | LOCKED-STATE-A | Aggregate-refusal wall — no person-key join [UNIVERSE_ZONE_MAP_V1 §7] | — | `docs/UNIVERSE_ZONE_MAP_V1.md` §7 · elab `docs/governance/READING_DOCTRINE.md` | — |
| L1 Project-Law | LOCKED | Serial = the address — object-keyed join spine (forced-split from provenance-arc) [GOLDEN_NUGGETS #8] | — | `docs/GOLDEN_NUGGETS.md` · `docs/UNIVERSE_ZONE_MAP_V1.md` §7 | — |
| L1 Project-Law | ACTIVE | Human-Read Line — image-act vs permanent; gates 1/2/3 (NOT-SPLIT) [HUMAN_READ_LINE_V1] | — | `docs/HUMAN_READ_LINE_V1.md` | — |
| L2 System/Subsystem-Spec | LOCKED | Halo Gate Boundary Lock [HALO_GATE_BOUNDARY_V1] | — | `docs/halo/HALO_GATE_BOUNDARY_V1.md` · `docs/DECISION_LOG.md` (2026-06-13) | — |
| L2 System/Subsystem-Spec | LOCKED | Base-Hex + Warm-Ramp Lock (parent of the two children below) [DECISION_LOG BR-S039] | — | `docs/DECISION_LOG.md` (2026-06-14) | — |
| L3 Surface-Spec | CONTEST | Final Halo material contest [CARD_TECH_LAB §20] | **[Black Star](docs/CARD_TECH_LAB.md)** · **[Night Gloss](docs/CARD_TECH_LAB.md)** · [Cold Foil](docs/CARD_TECH_LAB.md) | `docs/CARD_TECH_LAB.md` §20 · `docs/TASK_QUEUE.md` Backlog | ≥4 BR-S (due) |
| L3 Surface-Spec | ACTIVE | Dossier de-dull — record reads as one filed object [DECISION_LOG BR-S048] | — | `docs/DECISION_LOG.md` (BR-S048) · `docs/research/LAYUP_RESEARCH_V1.md` | — |
| L4 Element/Technique | ACTIVE | Provenance verb thread (child of provenance-arc split) [UNIVERSE_ZONE_MAP_V1 §3] | — | `docs/UNIVERSE_ZONE_MAP_V1.md` §3 | — |
| L4 Element/Technique | ACTIVE | Tier-ratio re-derivation (child of Base-Hex lock) [DECISION_LOG BR-S055] | — | `docs/DECISION_LOG.md` (BR-S055) | — |
| L4 Element/Technique | ACTIVE | Plate-bg consolidation (child of Base-Hex lock) [DECISION_LOG BR-S056] | — | `docs/DECISION_LOG.md` (BR-S056) | — |
| L5 Detail-Cue | ACTIVE | Colophon capture date on Plate 06 — LATER beat (child of provenance-arc split) [UNIVERSE_ZONE_MAP_V1 §3] | — | `docs/UNIVERSE_ZONE_MAP_V1.md` §3 | — |
| L5 Detail-Cue | ACTIVE | Menu "what this is" hint line [DECISION_LOG BR-S053] | — | `docs/DECISION_LOG.md` (BR-S053) · `docs/COPY_SYSTEM.md` | — |

{
 "subsystem": "Arcana detail-page section order (identity vs symbolic systems) + 5 new dimensions",
 "decision": "Four tiers, MEANING → STRUCTURE → DEPTH → HISTORY. Masthead absorbs \"The-reading(codex)\" and \"At-a-glance\" as components, not scroll-stops. \"Attributions\" is killed as a standalone section and folded into the Origin ledger + Sources footer. Compatibility/In-love/Friendships apply only to identity systems (sun signs, Chinese zodiac, life path); symbolic systems (tarot, runes, hexagrams, trigrams) skip them entirely rather than getting a forced stub. \"What-it-turns-on\" is promoted to the single most load-bearing line on the page — the one place a sliver of gold-life can land without breaking gold-rationing, because it's already the page's emotional pivot, not decoration.",
 "specifics": [
  "TIER 1 — MEANING (always open, ~90 words, the skimmer's whole visit): 1) MASTHEAD — name/tag/glyph/keywords chips, with the old flat `meaning` field demoted to a collapsed 'standing gloss' toggle inside it (this is where 'The-reading(codex)' content lives now — it is never a separate section). At-a-glance data (element/ruler/quick facfacts) renders as a factbar row inside the masthead, not its own scroll-stop. 2) ESSENCE (new) — the cold-open, 2-4 sentences, archetype-not-reader voice, banned-opener + same-system swap test apply. Replaces the generic intro every page currently lacks. 3) GIFTS (renamed from Strengths) — flat attributed properties, ledger paragraph not a checklist (checklists read like a stat sheet). 4) HARDSHIPS (renamed from Shadow) — same treatment, positioned immediately after Gifts as a pair, never opposed as pro/con columns. 5) WHAT-IT-TURNS-ON — kept, promoted: one pull-quoted mechanism sentence, the single most engaging and most quotable unit on the page (e.g. the Aries 'turns on beginning before consensus' example). This is the one place allowed a restrained accent treatment since it IS the meaning payoff, not chrome.",
  "TIER 2 — STRUCTURE / RELATIONSHIPS (open by default for identity systems; entire tier omitted, not collapsed-empty, for symbolic systems): 6) FRIENDSHIPS (new) — social/platonic register, identity systems only. 7) IN-LOVE — romantic register, identity systems only. 8) COMPATIBILITY — paired-archetype dynamics, identity systems only, framed as 'reads well against' not scored pairing. Applicability rule: tarot/runes/hexagrams/trigrams have no natural social register in the tradition — do not manufacture one; this whole tier is a clean per-system skip, which is itself the anti-bloat move (no stub, no 'N/A').",
  "TIER 3 — DEPTH (interested reader: headings + first line visible, one-tap expand): 9) CORRESPONDENCES — the tiered symbolic grid (element/planet/letter/trigram/branch), every cell attestation-stamped. 10) DEEPER-HOOKS (kept as-is, unmerged) — the mandatory anti-template unique-fact section; its hook.slot rotation is a structural distinctness mechanism and must stay a separate render target even though thematically close to Correspondences.",
  "TIER 4 — HISTORY (researcher tier, collapsed by default, payload-labeled e.g. '3 disputed points', true end-of-page): 11) VARIATIONS (new) — naming history, regional/textual variants, alternate decks/poems/translations across eras; this is the historical-divergence record, not a meaning dimension, so it opens History rather than sitting in Tier 1-2. 12) ORIGIN (the provenance spine/timeline, era→who→what-changed ledger; absorbs what 'Attributions' used to try to do as a separate section — the per-layer tier stamp IS the attribution, so a standalone Attributions block is redundant and should be cut). 13) CONTESTED — dated dispute narration, no verdict, absent block is itself information. 14) SOURCES footer (not in the original list but required by the existing schema to back every tier stamp — add it as the true terminus, no 'close'/summary after it).",
  "CUTS/MERGES: 'The-reading(codex)' -> folded into Masthead's collapsed standing-gloss toggle (never restated in body). 'At-a-glance' -> folded into Masthead factbar (component, not section). 'Attributions' -> killed as standalone, absorbed into Origin's per-layer tier stamps + Sources footer. 'Compatibility'/'In-love'/'Friendships' -> per-system conditional, fully omitted (not stubbed) for the four symbolic systems.",
  "APPLICABILITY MAP: Identity systems (Western sun signs, Chinese zodiac animals, Life Path numbers) get all 14 sections/components. Symbolic systems (Tarot Major, Runes, I Ching hexagrams, Trigrams) get everything except Friendships/In-love/Compatibility — Tier 2 collapses to zero width for them, which is fine and expected, not a gap to fill.",
  "ENGAGEMENT MOVES WITHOUT BLOAT: keep Gifts/Hardships as prose paragraphs (2-3 sentences each), never bulleted checklists — bullets read as a stat block, exactly what canon rejected. Give What-it-turns-on its own visual beat (larger leading, not larger size, per the type-hierarchy rule already in canon) since it's the one sentence most likely to be screenshotted/shared — that's the 'sleek colour-life' opportunity: a single quiet warm accent on this one line only, never repeated elsewhere on the page, so gold/ember rationing stays intact. Variations should open with one concrete divergent fact (a renamed card, a substitute animal, a different rune-poem stanza) rather than a generic 'this varies by culture' sentence — same swap-test discipline as the cold open."
 ],
 "exact_values": [
  "FINAL ORDER: Masthead(w/ standing-gloss + at-a-glance factbar) -> Essence -> Gifts -> Hardships -> What-it-turns-on -> [Friendships -> In-love -> Compatibility, identity-systems only] -> Correspondences -> Deeper-hooks -> Variations -> Origin(provenance spine) -> Contested -> Sources",
  "Identity systems (get full 14-part page): western_sun_signs, chinese_zodiac, numerology/life-path",
  "Symbolic systems (skip Friendships/In-love/Compatibility entirely): tarot_major, runes_elder_futhark, trigrams, hexagrams",
  "Killed as standalone sections: 'The-reading(codex)' (-> masthead toggle), 'At-a-glance' (-> masthead factbar component), 'Attributions' (-> merged into Origin's per-layer tier stamps + Sources footer)"
 ],
 "risks": [
  "If Gifts/Hardships get bulleted or icon-marked instead of prose, they will read as a stat sheet/scorecard — exactly the gamey pattern the client already rejected for 7-colour coding.",
  "Promoting What-it-turns-on with any accent risks becoming a second gold-bearer if not tightly scoped to that single line, breaking the existing gold-rationing rule (crown + one pressed reading only, per the reading-page direction doc).",
  "Omitting Tier 2 for symbolic systems must be a clean skip in the renderer, not a collapsed-but-present empty accordion — an empty 'Compatibility' stub for The Tower would read as a bug, not a design choice.",
  "Variations and Origin are adjacent and both historical — without a firm content rule (Variations = named textual/cultural divergences, Origin = dated chronological ledger) writers will duplicate material between them."
 ],
 "highest_leverage": "Promote 'What-it-turns-on' out of the middle of the list into the closing line of Tier 1 (right after Gifts/Hardships) and give it the page's one sanctioned accent treatment — it is already the single sentence that makes an archetype legible and memorable, so it is the correct (and only) place to spend the client's requested 'sleek colour-life' without touching the gold/ember rationing that governs everything else."
}
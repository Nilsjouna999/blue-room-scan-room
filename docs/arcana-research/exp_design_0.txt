{
 "subsystem": "Colour system — element-tint accents for Arcana result pages",
 "decision": "Adopt a single ELEMENT-TINT per result: one restrained accent hue tied to the result's classical element, spent on exactly one small surface. Confirmed against docs/DESIGN_TOKENS.md (\"Accent is currency, not paint\" + \"one palette speaks at a time\") — this is a third accent lane alongside Gold and Ember, but it is gated to a single surface per page so it never becomes a second currency or a rainbow legend.",
 "specifics": [
  "THE ONE RULE (reverent, not gamey): one tint, one surface, one instance, never text. Exactly one small non-text surface per page carries the element tint (badge fill, emblem ring, or section hairline) — never more than one location lit at once, never a repeating legend/key that teaches the reader a colour-code, and never used to imply grade, rank, or verdict the way a status colour would. It marks WHERE the tradition's material sits on the page, not HOW GOOD the result is.",
  "WHERE COLOUR IS ALLOWED (pick exactly one per page): (a) the element badge's fill or thin ring, (b) a 1-2px ring around the hero emblem (outside the gold, never replacing it), (c) a hairline top-rule or small dot marking the element/compatibility section header. WHERE IT IS FORBIDDEN: prose/body text, strengths/gifts/hardships copy, the card's own material (gold/copper/frost stay untouched), buttons/CTAs, headers/titles, more than one surface at a time, any two elements visible together on one page.",
  "WESTERN FOUR (softened for near-black, same desaturated archive character as Gold #c9a35c / Ember #b25f39 — mid-low lightness, moderate saturation, none brighter than gold): Fire #9c5a34 (burnt clay-orange — deliberately shifted warmer/lighter than Ember's brick-red so the reversed-read colour and the Fire-element colour are never mistaken for each other), Earth #6f6a42 (dry moss-khaki, olive-brown), Air #6d8791 (dusted slate-cyan, pale but not bright), Water #3e5f73 (deep harbor-blue, muted ink-teal).",
  "CHINESE FIVE (Wu Xing): Wood #4c6f52 (muted pine-jade green, new), Fire #9c5a34 (shared with Western Fire — same classical element, same tint, no duplicate hue needed), Earth #7a6234 (dark ochre-umber — kept distinct from Western Earth's olive AND from Gold #c9a35c by sitting darker/browner than both), Metal #7f8790 (cool pewter-grey — deliberately cooler than the warm Sand text ramp's Meta grey #948f87 so it never reads as a text tone), Water #33495a (inkier/darker than Western Water to carry the more yin, near-black reading Chinese Water traditionally carries).",
  "Contrast/competition check: every tint sits at or below Gold's lightness (~57% HSL) and below Ember's saturation, so on the #0a0b0d ground none of the eight hexes competes with Gold-as-currency or Ember-as-clash-signal; they read as a quieter third register, only ever one at a time.",
  "HOVER / MICRO-TINT LIFE: on the one interactive instance of the tinted surface (e.g. hovering the element badge or a compatibility chip), allow a brief +6-8% lightness lift on that same hue as the only motion-of-colour permitted — confirms interactivity, never introduces a new hue, reverts fully on blur/mouse-leave, and never appears on non-interactive surfaces (prose, section body) or persists as a new resting state.",
  "Reference: canon confirmed in C:\\Users\\nilsj\\OneDrive\\Documents\\blue-room-scan-room\\docs\\DESIGN_TOKENS.md (base #0a0b0d lock, warm Sand text ramp #e9e5dc/#d7d3ca/#b1ada4/#948f87/#6e6b63, 'accent is currency not paint', 'one palette speaks at a time' rule which this system is designed to not violate by staying single-surface)."
 ],
 "risks": [
  "If a future pass adds a second tinted surface per page (e.g. badge AND section rule both lit), it silently becomes a rainbow-coding system again — the one-surface rule needs to be enforced at the component level, not just at spec time.",
  "Chinese Fire and Western Fire sharing one hex is a simplification; if the product ever needs to distinguish which cosmology a result belongs to at a glance, this collapses that signal — flagged as intentional, not an oversight.",
  "Metal #7f8790 and Air #6d8791 are close in hue/lightness (both cool slate-grey); if a result could ever show both Metal and Air context on one page (unlikely under the one-tint rule but worth checking against the actual result-taxonomy), they'd be hard to tell apart — mitigated only by the one-tint-per-page rule holding."
 ]
}
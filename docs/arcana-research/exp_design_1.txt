{
 "subsystem": "Arcana Result Detail Page — interpretive dimensions, diagrams, and GPT-authored imagery (blue-room-scan-room)",
 "decision": "Proposal only, no code changed. Ground truth pulled from three files: docs/BR-ARCANA-RESULT-MARKS-ART-BRIEF_V1.md (the 154 sealed-mark system: 96x96 SVG, currentColor only, emblem+9-bit-cipher grammar, gold/ember rules), docs/DESIGN_TOKENS.md (color law, type tiers), and docs/BR-ARCANA-READING-MASTERPIECE-DIRECTION_V1.md (gold-as-currency discipline, \"subtractive not additive\" test, ember's one reserved role = reversed/shadow). Recommend 4 diagrams (one is a bonus that covers all 7 systems, not just the 3 named candidates) plus a hero-image slot, all archival/inline-SVG except the one GPT hero image per result.",
 "specifics": [
  "PAGE REORDER (meaning first, history last): 1) Title + kicker  2) GPT hero image (new)  3) Soul-Signature diagram (new, see below) — the page's own visual crest, sits where a portrait would  4) Gifts/Strengths (archetype-voiced properties)  5) Hardships (same voice, same flatness)  6) System-native structural diagram — Elemental Cycle for Chinese year-animal, Decan Strip for zodiac (new; explains WHY the properties hold, so it is meaning not decoration)  7) Compatibility wheel (existing graph, unchanged)  8) Numerology Reduction Geometry for life-path results (new — this one is mechanism/provenance, so it sits late even though it's a 'meaning' page, right before history)  9) Dry history / textual origin of the tradition — moved to the very end for deep-divers, per the brief.",
  "DIAGRAM 1 — Elemental Cycle (Wu Xing generative+controlling), for Chinese year-animal results. WHAT: the five-phase ring (Wood→Fire→Earth→Metal→Water→Wood, the generative/sheng cycle) with the inner five-pointed controlling star (ke cycle: Wood controls Earth, Earth controls Water, Water controls Fire, Fire controls Metal, Metal controls Wood) drawn as a pentagon. The entry's own fixed element (e.g. Snake=Fire, Dragon=Earth per the existing zodiac data) is the one gold node; its two touching generative arcs go gold; everything else stays ghost-grey line art. WHY TRUE: this is the actual 2000-year-old Wu Xing schema used across Chinese cosmology, not invented for the app — the diagram is generic (same 5 nodes/10 relationships every time), only the highlighted node changes per entry, exactly like the sealed-mark emblem/cipher discipline. Caption in archive voice: 'The tradition places [Animal] under Fire, which feeds Earth and is checked by Water.' HOW DRAWN: single inline SVG, dark ground, viewBox ~200x200, 5 nodes at 72° intervals on a circle (outer generative ring = solid arcs, warm-grey at rest, currentColor=gold only on the two arcs touching the highlighted node), inner pentagon/star = the controlling lines, thin dashed warm-grey (never ember — ember stays reserved for the reversed-read dot per canon). One 1px hairline outer ring for containment, matching the sealed-mark's hairline-divider language. WHERE: directly after Hardships, before the compatibility wheel — it is the structural 'why' beneath the flatly-stated properties.",
  "DIAGRAM 2 — Decan Strip, for zodiac sun-sign results. WHAT: the sign's 30° arc divided into its 3 traditional Chaldean decans (10° each), each labeled with its ruling sub-planet glyph (e.g. Aries: Mars / Sun / Venus). Because the app knows only the Sun sign (not exact birth date/time), the diagram deliberately shows all three decans as three facets of the one archetype rather than pinning the reader to one — this keeps it archive-of-the-sign, not a reading of the person, and sidesteps a data dependency the app doesn't have. WHY TRUE: Chaldean decan rulership is a real, centuries-old subdivision used in traditional astrology; attributing it ('tradition subdivides Aries into a Mars decan, a Sun decan, a Venus decan') is accurate without overclaiming personal specificity. HOW DRAWN: a horizontal strip, viewBox ~300x60, 3 equal segments separated by 1px hairlines (reusing the sealed-mark's hairline-divider grammar), each segment holding its ruling-planet Unicode glyph in ghost-grey currentColor, gold spent only on the strip's own frame/segment labels per the 'gold = slot labels' currency rule — never as a fill. HOW IT AVOIDS BEING A DASHBOARD: no bars, no percentages, no scoring — it is a filed subdivision, read left to right like a filmstrip. WHERE: same slot as Diagram 1 but for the zodiac system — directly after Hardships, before the compatibility wheel.",
  "DIAGRAM 3 — Numerology Reduction Geometry, for life-path number results. WHAT: the actual digit-sum reduction path from the birth-date digits down to the root number, drawn as a stepped descent of circles (one circle per reduction pass, each holding the intermediate sum), connected by a single hairline, terminating in the final numeral — reusing the identical geometric-numeral glyph already specified as the emblem in the sealed-mark system (docs/BR-ARCANA-RESULT-MARKS-ART-BRIEF_V1.md §5C). Master numbers 11/22/33 show an explicit fork/notch at the step where the tradition halts reduction instead of continuing to a single digit. WHY TRUE: this is not interpretation, it is the literal arithmetic the tradition uses to arrive at the number — showing the derivation is showing a fact, and doubles as a quiet proof that the number wasn't picked arbitrarily. HOW DRAWN: inline SVG, vertical stepped path, circles ~24px each at 96px canvas scale (same stroke weight/margin discipline as the sealed marks: 2px stroke, 8px clear margin), gold reserved for the terminal circle only (the 'currency' spent on the arrived-at result), every intermediate step ghost-grey. WHERE: this is mechanism/provenance rather than meaning, so per the reordering directive it sits late — immediately before the dry-history section, functioning as the bridge from 'what this means' into 'how the tradition computes it.'",
  "DIAGRAM 4 (bonus, covers all 7 systems incl. tarot/runes/trigram/hexagram, which the other 3 candidates don't reach) — the Soul-Signature Bloom. WHAT: a radial 9-spoke diagram generated from the exact same 9-bit cipher already computed for that entry's sealed mark (first 9 bits of SHA-1(entry name), Appendix tables in BR-ARCANA-RESULT-MARKS-ART-BRIEF_V1.md) — 9 spokes at 40° intervals from a center point, each ending in a filled disc (bit=1) or open ring (bit=0) in the identical fixed row-major order the mark already uses, just expanded from a 3x3 grid to a circle. WHY THIS IS GENUINE, NOT DECORATIVE: it introduces zero new data — it is the literal fingerprint already computed and already used elsewhere on the same page (the small sealed mark), rendered at hero scale. This mirrors the product's own 'same object, no addition' law (the crown appears twice from the same markup) applied to the mark instead of the crown. It also explains why marks intentionally collide (two entries can share a cipher) without the page having to say so in prose. HOW DRAWN: SVG viewBox ~200x200, center circle (currentColor ghost-grey), 9 spokes fixed at 40° steps, terminal disc/ring per bit, 2px stroke to match the sealed-mark stroke weight so the two objects visually rhyme, gold permitted here (unlike the sealed mark itself, which per §4 of the brief must contain literal zero color) because this is a page-level render where the container, not the file, supplies color — same mechanism the brief already specifies for hero/focus states. WHERE: directly under the title, replacing what would otherwise be a portrait — first thing seen, the page's own crest, before any meaning-copy.",
  "GPT HERO IMAGE — placement: one hero image per result, sitting directly beneath the Soul-Signature bloom and above the Gifts/Strengths section (the one zone on the page where painterly/raster imagery is sanctioned; every other visual stays inline SVG line art per canon). If a full 154-image budget isn't approved, fall back to 7 generic per-system heroes (one look per system, not per entry) rather than mixing raster and vector inconsistently — flag this as an open budget decision, not a design one.",
  "GPT HERO ART-BRIEF TEMPLATE (one paragraph, fill the bracketed fields per entry): 'Render a single symbolic motif for [ENTRY NAME], the [SYSTEM] archetype — not a portrait, not a scene, not a mascot, and no human figure anywhere in frame: a real object or natural form the tradition itself associates with [ENTRY], photographed as if it were a recovered museum specimen at night. Ground: near-black fading to warm archive grey (#0a0b0d ceiling to #b1ada4 body-grey, no pure black or white). One low, raking light source; camera-real texture — grain, wear, imperfection — never flat vector, never cartoon, never a glowing/mythic treatment. No gold and no bright color baked into the image (gold is spent by the page chrome around it, never inside the art itself); no text, no border, no vignette frame, no lens flare. The subject fills roughly 55–65% of frame, centered or slightly off-center, mood reverent-clinical like an object under glass — never heroic, never cute, never triumphant. Example fill: Aries → a ram's horn, cracked, mounted on a velvet specimen block; the Snake → a single shed snakeskin coiled on cold stone.' This mirrors the existing marks brief's own discipline (attribute, don't invent; restraint is the entire brief) applied to a photographic register instead of a vector one.",
  "CANON GUARDRAILS CARRIED FORWARD INTO ALL 4 DIAGRAMS: (1) gold appears only on the one highlighted/terminal element per diagram, never as a fill or wash — consistent with 'gold is currency, spent only on crown + slot labels + focus'; (2) ember never appears in any of these diagrams — its one reserved role stays the reversed-read indicator, added at runtime, never baked into a diagram file; (3) every diagram is inline SVG on the near-black ground, currentColor-driven where it can be (so it can sit quiet-grey at rest and gold on focus, exactly like the sealed marks); (4) no percentages, no scores, no bars-as-ratings — the shapes encode real computed or traditional structure (a cipher, a rulership table, an arithmetic reduction, a cosmological cycle), never a synthetic 'compatibility %'; (5) captions stay in the deadpan-archival voice specified in this task — 'the tradition places,' 'the tradition subdivides,' 'the tradition halts reduction at 11/22/33' — never second person, never a verdict on the reader."
 ],
 "risks": [
  "Decan Strip has a real data gap: without exact birth date/time the app cannot know which single decan applies, so it must show all three as facets of the sign rather than pretend precision — if product later adds birthdate input, revisit to highlight one decan in gold instead of all three neutral.",
  "Soul-Signature Bloom deliberately breaks the sealed-mark brief's 'no color in the file' rule by allowing gold at hero scale — this must be page-supplied currentColor exactly as the brief already permits for the small mark's hero-context (§4), not a second colored asset; if implemented as a separate baked SVG file, it will violate §4 and fail review.",
  "If the GPT hero-image budget is 154 unique renders, consistency drift across generations is a real risk (the marks brief needed a prototype-gate + worked-examples process for exactly this reason) — recommend the same prototype-then-batch discipline: draw 1, lock the look, then batch by system.",
  "Wu Xing and Decan diagrams both assume the underlying per-entry element/decan data already exists in the arcana knowledge base (arcana-knowledge.json) — this wasn't verified line-by-line in this pass; confirm the element field is present for all 12 year-animals before implementation."
 ],
 "highest_leverage": "Diagram 4 (Soul-Signature Bloom) is the highest-leverage addition: it costs zero new data or research (it's the identical 9-bit cipher already computed and drawn for the 154 sealed marks), covers all 7 systems including the 4 the other candidates miss (tarot, runes, trigram, hexagram), and it structurally mirrors the product's own signature move (the crown rendered twice from one markup, 'never improved, only unveiled') — applying that same discipline to the per-result mark rather than inventing a new decorative graphic.",
 "exact_values": [
  "Ground: #0a0b0d (--ink-950)",
  "Text ramp: display #e9e5dc / primary #d7d3ca / body #b1ada4 / meta #948f87 / ghost #6e6b63",
  "Gold currency: #c9a35c base, #e2c489 bright",
  "Ember (reversed/shadow only, never baked into diagrams): #b25f39",
  "Sealed-mark canvas: 96x96 viewBox, 2px stroke, 8px clear margin, currentColor only — reused stroke/margin values recommended for Diagrams 3 and 4 so the new graphics visually rhyme with the existing 154 marks",
  "Cipher source: first 9 bits of SHA-1(entry name, UTF-8), row-major, 1=filled disc/0=open ring — exact per-entry values already tabulated in docs/BR-ARCANA-RESULT-MARKS-ART-BRIEF_V1.md Appendix A-G"
 ]
}
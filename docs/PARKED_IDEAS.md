# BLUE ROOM — Parked Ideas (holding pen)

Parked ideas — solid, not ranked, not for action until core (scan→mint) ships. Raw capture only.

> Core first: the scan→mint card is the product and must ship before any of these are worked.
> This is a raw list — no states, no ranks, no fit-tests. Capture, then leave alone.

## The ideas

- **Codex / info hub** — numerology, zodiac, Chinese zodiac, and MBTI as serious content (a real
  info/lore layer, not a gimmick). Carries the former PROJECT_OS §15 "Profile Layers" note:
  **user-provided / declared, never inferred from the photo** — "the photo creates the card; the
  profile adds the lore."
- **Card autobattler with friend cards** — minted cards become pieces in a small battler; interactive
  and/or combat, undecided.
- **Social presence** — connect with others, online/offline, messaging; shape undecided.
- **Chill / idle space** — a room to just exist in the universe, no task.

- *(room for more — add a line above when an idea is solid.)*

## Guardrail (carry once)

Any of these that ever touches a Vault / codex / social layer stays **object-keyed** (scan serial →
filing-event → mint #), **never person-keyed**. (Inherits the `UNIVERSE_ZONE_MAP_V1` §7 aggregate-refusal
wall: no person-key join; re-derive fresh per scan.)

## What this could become (signpost only — build nothing yet)

This raw pen can later grow — when I choose to work it — into a fuller idea workspace:
- add STATES (wild → possible → planned → building → shipped → rejected)
- add a FIT TEST against the fixed core (does it build on the minted card, or is it a separate project
  wearing Blue Room's name?)
- add a GATE rule (nothing advances past "possible" until core ships)
- add rough RANKING once the pile is big enough to need cutting
- at that point, White Death is the tool to cut the pile to a shortlist
- CK3 (objects carrying lineage, portraits carrying history) is the reference to read WHEN rooms / vault /
  social actually get designed — not before

Until then this stays a raw list. Do not build any of the above yet.

# BLUE ROOM — Research Synthesis v1 (the canon)

Layer: RESEARCH (informs, never implements — GOVERNANCE_OS §3). Date: 2026-06-14.
Source: a 19-agent White Death AUDIT over all 15 research docs (one Sonnet distiller per
doc → Opus consolidate/dedup → Opus works-together + Opus conflict-hunter → Opus rank,
alone). No new sourcing. **This doc is the reconciliation layer that sits atop the
others** — what the whole research body concluded, where it agrees, and where it clashes.

**Verdict:** 64 high-value + high-confidence survivors (54 KEEP, 10 DILUTE), **62 killed.**
The survivors **cohere as one system** — every clash below is a CSS/copy/spec reconciliation,
not a model contradiction. The full 64-point canon lives distributed across the 15 source
docs; this synthesis captures the structure (9 systems), the clashes-with-resolutions (the
build-blocking gold), and the build-now-vs-later split.

## The research = 9 coherent systems
1. **The Master Law Stack** — Master Law → Five-gate copy test → Hard-banned build list →
   Final Product Law (10-line mantra). One ethical engine at four altitudes (principle /
   copy-checklist / build-floor / proposal gate). The two-photo test is the sharpest decision
   instrument.
2. **The Closed Language System** — allowed/banned vocabulary + rewrite pairs + receipt schema
   (`cue → effect`, {cue,effect,basis,confidence}) + voice/tone ladder + word-bank. A closed
   loop with no gap for person-appraisal language to re-enter.
3. **The Three-Layer Scan Engine** — Intake Gate (accept/limit/reject) → Route (9 routes) →
   Develop (title/archetype/stats/hidden/receipts/rarity/oracle/dossier). The buildable pipeline.
4. **The Honest Free/Paid Seam** — kind-not-amount split + complete-Free-front + single
   back-seam gate + funnel timing + the latent→developed law that makes it read honest, not paywall.
5. **The Depth Physics** — three planes, one 12-o'clock light source, multi-layer shadow/glow,
   **card-confined** (only the card has depth). *(Internally inconsistent until the plate-depth
   clash is resolved — see Conflicts.)*
6. **The Typographic Hierarchy System** — 5-tier contrast + warm hue + font roles + tabular/
   minted numbers + serial grammar + one-focal-point + layer-cake kickers.
7. **The Engineered-Instrument Chrome** — soft-SaaS → archive instrument via line/number/type/
   rhythm only (meters, chrome-conversion, dim-floor, counting spine, nav unification, Source
   merge, Diagram polish, Metrics redesign).
8. **The Dossier Pacing System** — loudness sequence + pacing tokens + colophon end-mark (the
   dossier carries the depth the card refuses to).
9. **The Interconnection Spine** — serial = the join key (HOW surfaces connect); archetype
   class = the content axis (WHAT each says); two-track attachment law; filing-event chain;
   field-token through-thread. Plus the **Archive universe frame** (flat zones, logbook vault,
   CSS-confined flip) as the felt-universe wrapper.

## Merges (fold these)
- **Rare-Artifact Determinism System** = the configuration-class ladder + the rare-trigger
  dictionary are input/output of one mechanism (triggers fire → configuration class → public
  signal-strength tier band). One axis, not two.
- **Latent→Developed law (incl. the Oracle close)** = the cosmological law + Develop-as-complete-
  chapter + Oracle-as-chapter-close are the same anti-compulsion narrative law; the Oracle close
  is its terminal case. One canonical point.

## Conflicts to resolve BEFORE building (every clash + its resolution)
**HARD (must reconcile or the system breaks):**
- **Plate depth:** ARCHIVE_LUXURY_DEPTH gives plates light-catching depth; LAYUP forbids any
  surround depth. → **LAYUP wins** (later, user-grounded). Plates get *edge + tone only* (not
  depth); kill `.plate--raised`; multi-layer shadow/glow/inset stays **card-only**.
- **Free-front "counts":** research says "shape/count only"; HALO_GATE_BOUNDARY §B bans exact
  pre-unlock counts. → **Strike "count"** — Free shows *qualitative existence* ("a sealed map
  layer exists"), never an enumerated number.
- **0–100 vs tier bands:** a 0–100 stat formula vs PROJECT_OS §9 tier-bands-no-0-100. → 0–100 is
  **internal only**; the Free surface renders **tier bands** (≤4 stats); "six stats" = the
  catalog of types, not six public numbers.
- **Accent location:** "the one gradient = Hidden Stat" vs "the one surround accent = Oracle
  edge." → Separate **artifact-accent** (the Hidden-Stat climax *number* may carry the gradient)
  from **surround-accent** (exactly one: the Oracle-plate left *edge* line). No surround fills.
- **Token-ramp duplication:** `--ink-*` (warm) vs `--vault-*` (cool) for the same greys; 3
  different plate-bg hexes. → Unify to **one warm Sand-tinted ramp**; pick **one plate-bg hex**
  and re-tune the engraved text-shadow to it before build.
- **Base background mismatch:** Vault ratios computed on `#0c0d10`; depth ships `#0a0b0d`. →
  Lock **one base hex** (`#0a0b0d`), re-derive the tier ratios against it.
- **Warm vs cool surfaces:** TEXT mandates warm Sand neutrals; depth tokens are blue-leaning. →
  **Warm-neutral rule wins** for all non-frost surfaces; cool only for the frost material + violet bridge.
- **Glow keyed to PAID:** depth makes the paid card visually superior; FREE_PAID says Halo is a
  *mode* not a better grade. → Glow/depth keys off **RARE** (a config property that can appear
  on Free), **never paid-vs-free**; Halo adds depth-of-record, not a brighter object.
- **Halo defined by "deeper AI":** SPINE §6 vs no-AI-built + kind-not-amount. → Re-anchor Halo's
  value on **KIND/mode** (the sealed-back production record); AI is an eventual delivery
  mechanism, not the differentiator, and not built before the static grammar ships.

**TENSION (guard the wording):** humor is **HARD-OFF on all safety/intake/gate surfaces** (joke
register = accepted-scan reading only) · two rarity ladders → **one public axis** (signal-strength
bands) + internal config label · **lens count 7-vs-10 unreconciled** → ship the concept, lock the
count later (DILUTE) · mythic titles legal **only** when bound to a visible cue (floating
backstory/fate = banned) · add-structure vs demote-90% → all added marks live at **ghost tier** ·
two-track lore → the **forbidden-cross must be a release-blocking validator** (declared lore never
read by archetype/oracle/stat/aura) · colophon time = a **frozen past datum**, never a live timer ·
card material = **static first** (3D tilt/moving glare behind a later perf-gated, reduced-motion
phase) · `mix-blend` = **card-window-scoped only**, never full-screen · Hidden-Stat climax loud via
**type scale + whitespace** (no box/fill), below the fold so it never shares a viewport with the card.

## Build now vs later
**Now (copy/CSS/rules over live state, behind `baseline-v1`):** Master Law Stack + Closed Language
System as written review rules · **dim the packaging** (LAYUP #1) · **button/nav-frame unification**
(LAYUP #2) · three-plane depth + 12-o'clock light (plates flat) · accent economy (3/viewport) ·
font-roles + tabular numbers + one-focal-point — *after* locking one base hex + one warm ramp ·
dossier loudness + colophon (frozen-time) · engineered-instrument chrome pass · **Source→Diagram
merge** (LAYUP #4, re-point `?tab=source`) · the **Interconnection Spine** as rules + the
**stat-receipt / subject-lock validator** · Archive zones as nav reframing + the static Room
(light-pool + grid) · **static** five-axis card-material foils.

**Later (deferred by the build-order law):** the full scan engine (gate/route/lens/confidence —
spec now, build later; no AI/upload before static grammar) · pointer/3D card materiality
(perf-gated) · the Halo dossier + back-seam gate build (define now, build later; value = mode not
AI) · Reframe Map feature (validate the taxonomy first) · lens enum (reconcile 7-vs-10 first) ·
per-family trigger detection · Vault/Codex/Journal + gloss (note the seam, build the room later) ·
scroll-reveal/develop-wipe enhancements · the two-track declared-lore rider (ship only with the
forbidden-cross validator).

`KILLED: 62.`

## How to use this doc
This is the **single source of truth for what the research concluded.** Before building any
surface: (1) check it passes the Master Law Stack + Closed Language System; (2) check the
relevant Conflict resolution above (don't re-litigate — they're decided); (3) confirm it's in
the **Build-now** set, not Build-later. The 6 spine docs (universe · free-paid · layup · layering
· depth · magnetism) + the 9 foundational research docs remain the detail; this is the index +
reconciliation. The convergent first build remains the **filing-event row + serial reveal on
Plate 01** (endorsed by 4+ docs and a live Ready item).

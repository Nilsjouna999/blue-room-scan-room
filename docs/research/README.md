# BLUE ROOM — Research (distilled checkpoint)

Layer: RESEARCH · Status: reference only · Last saved: 2026-06-13 (BR-S028)

This folder is the **cloud checkpoint for distilled BLUE ROOM research** —
the artifact-language, stat/tier, human-in-frame, rare-trigger, Reframe Map,
and voice rules — so future GPT/Claude sessions do not forget them.

## How to use this folder

- It stores **compressed research for future product/coding decisions** —
  not current runtime law.
- **Raw research must not be pasted into normal coding tasks.** Read these
  files only when a task is actually making a **language / card / stat /
  rarity / Reframe** decision; otherwise leave them closed (FILE_MAP scope
  rule · GOVERNANCE_OS §3).
- **`RESEARCH_COMPRESSION_V1.md` is the compact source** — the one file to
  pull into a future coding/spec prompt. The separate `*_V1.md` docs below
  are **planning / spec references, not runtime implementation**.

## Authority (important — read before building from this)

These are **RESEARCH**, not ACTIVE specs. Per `docs/GOVERNANCE_OS.md` §3,
research **informs, never implements**: it flows raw → `SPINE.md` → an ACTIVE
spec → code. Nothing here changes runtime on its own.

Where this research **diverges** from current ACTIVE specs, the ACTIVE spec
wins until a migration task promotes the change. Known live divergences to
reconcile later (do **not** silently apply them):

- **Stat names** — research prefers Frame / Signal / Scene Charge / Lore
  Density; `CARD_SYSTEM_V1` §2 currently ships Presence / Frame / Signal /
  Visual Impact.
- **Tier ladder** — research prefers Muted → Clean → Strong → Charged → Peak;
  `CARD_LOGIC_V1` §2 ships QUIET / PRESENT / STRONG / DOMINANT / TOTAL and
  `FREE_PULL_SCREENSHOT_LAYOUT_V1` §7 recommends Quiet/Set/Strong/Sharp/
  Dominant. **Sample Room Tier Migration v1** owns ratifying one ladder.

## Files

- `RESEARCH_COMPRESSION_V1.md` — **the compact source.** Master law, Artifact
  Test, human-in-frame law, stat/tier language, Free Pull rules, receipt
  grammar, voice, rare-trigger families, Reframe Map law, roadmap.
- `HUMAN_IN_FRAME_READING_RULES_V1.md` — read the performance, not the
  performer; the five-gate test; bad→good rewrites.
- `RARE_ARTIFACT_TRIGGER_SYSTEM_V1.md` — rarity as a catalog note (12 trigger
  families); deterministic cue → trigger, no chance/packs/rerolls.
- `REFRAME_MAP_VALUE_SYSTEM_V1.md` — Reframe Map = the card's production
  notes; change the artifact, not the person; Free shows shape only.
- `LANGUAGE_SYSTEM_V1.md` — dry-cinematic voice, tone ladder, title grammar,
  word bank, receipt-chip grammar.

(The other files here — `SPINE.md`, `ARCHIVE_LUXURY_DEPTH.md`,
`DOSSIER_PACING_MINTED_NUMBERS.md`, `TEXT_EYE_TRAVEL.md` — are the earlier
raw/filtered research; `SPINE.md` stays the top-of-research spine.)

## Current next product task

**Free Pull Mock Polish v1** — after the Free Pull Screenshot Audit (PASS WITH
SMALL FIXES). It tightens the receipt chips, gives the dev fixture a real
artifact title, and aligns the archetype to the daylight stand-in image —
*before* Sample Room Tier Migration v1. This checkpoint does not change that
roadmap; it only saves the research behind it.

# BLUE ROOM — Layering Model v1 (coherence research)

Layer: RESEARCH (informs, never implements — GOVERNANCE_OS §3). Date: 2026-06-14.
Source: a 15-agent White Death SCOUT on BLUE ROOM layering (6 ground the grammar → Opus
builds a latent-link substrate → 6 scout candidate layers → Opus FIT-adversary → Opus
ranks alone). **FIT was the bar, not value.** Approves nothing; pull a layer via the
normal pre-edit critique, one commit behind `baseline-v1`.

**Headline:** the coherent universe is **not** new features — it is **three load-bearing
seams that already exist in the data and only need to be *surfaced as links*.** Every
survivor is connective-only over verified fixture facts: **no detector, no new schema
field, no grammar-bend.** The kill-bar did its job — anything needing a backstory, a
detector, or ungoverned copy floated and died.

---

## The 4 priority seams (the spine)

- **P1 — ARCHETYPE BRIDGE (keystone).** The 12 narrative classes (COPY_SYSTEM §4), the 24
  compositional archetypes / 5 families (CARD_SYSTEM_V1 §4), the per-instance titles, and
  the hidden-stat directions all live in the data with **no connecting table**
  (`V2_EXTRAS.archetype.routeLogic` cites only "strongest stat pair", names no family/lens/
  hidden-stat). One mapping object (class → family → lens pair → hidden-stat direction)
  turns three fixture coincidences into one rule and becomes the spine running through
  class→hidden-stat, triggers→family, class→Codex, class→voice-register. Highest
  depth-per-gram.
- **P2 — PROVENANCE LINEAGE (one object, one chain).** The IDs are already linked in code
  but invisible: `scanId` is computed from `objectNo` (data.js:473); the masked Free
  serial `····` (app.js:853) is literally the placeholder slot the real Halo serial
  (app.js:845) fills; `capture.code → route → archetypeClass` is one causal chain split
  across three surfaces. A provenance through-line on Plate 01/06 makes **Develop a
  visible identity event** (sealed-at-Free → assigned-at-Halo), not a tier gate. Zero new
  data.
- **P3 — EVIDENCE → STAT → DISPLAY CLOSED LOOP (the grounding seam).** `confidence.overall`
  already equals the "Subject lock" receipt (stated in the data.js:430 comment), feeds the
  shrinkage formula `displayed = 50 + confidence*(raw-50)` into every tier band; `fitMatrix`
  states are the unused receipt evidence for Fit Coherence; the fixture already wires
  `publicStats[i].receiptId → evidenceBoard[i].id` but the validator never enforces it.
  Formalizing "every displayed stat carries a receiptId resolving to a board entry, and
  confidence is the same datum as its lock receipt" closes the loop with no new machinery.
- **P4 — SINGLE BACK-SEAM GATE (the surface, not a layer).** Concentrating the smeared
  free/paid gate at the Card Record back-seam (per FREE_PAID_MODEL_V1) is where the Develop
  verb + latent↔developed flip + sealed-back copy + Halo material all converge. Scouts treat
  this as the surface their layers land **on** — it activates P2 (serial reveal), the oracle
  timeline teaser, and the Codex/Vault seams.

## Ranked survivors (final cross-pass FIT cut)

| # | Layer | Maps to / links | Verdict · Conf |
|---|---|---|---|
| 1 | **Serial lineage through-line** (P2) | Plate 01 Source Record + Plate 06 Mint Record; `objectNo → scanId → card.serial → mint serial`; masked Free `····` → assigned Halo serial | **KEEP · High** |
| 2 | **Class-to-family mapping table** (P1 keystone) | `ARCHETYPE_NOTES` (class-keyed) + `routeLogic` + COPY_SYSTEM §4 + CARD_SYSTEM_V1 §4; class → family → lens pair → hidden-stat | **KEEP · High** |
| 3 | **Route + scan-status rows on Plate 01** | `V2_EXTRAS.route` + `scanStatus` → Source Record DL; SCAN_ROUTING_SPEC mandates the Free route label (absent today) | **KEEP · High** |
| 4 | **Stat-receipt closure** (validator rule) | `publicStat.receiptId → evidenceBoard.id`; scan-contract validator enforces it (fixture already wires it) | **KEEP · High** |
| 5 | **Subject-lock = confidence.overall** (shrinkage provenance) | "Subject lock" receipt = `confidence.overall` → shrinkage → every tier band (identity already in a code comment) | **KEEP · High** |
| 6 | **"FILED-AS" caption on Plate 01** | `capture.code → route → class → discovery note`; grounds the "filed" note in capture facts | **KEEP · Med** (first to drop if Plate 01 crowds) |
| 7 | capture.rec ≡ card.minted → **colophon end-mark** | Plate 06 close; same date on two plates; specced colophon | **DILUTE** (fold into #1 — the chain's closing stamp) |
| 8 | mint-trigger pair → discovery note on Plate 06 | trigger1/2/family + class note | **DILUTE** (fold into #2 — don't third-surface the note) |

`KILLED: 5` (hidden) — the floating ones: aura-derivation rule (BENDS — aura is ungoverned
copy, a person-drift open door), horizon→scene-role rule (BENDS), oracle-timeline-as-variant
(floats to invented mythology), Codex/Vault-dependent layers (future-only = WHY-RULE kill),
premature lens-vocab enum (bends an unreconciled 7-vs-10-lens boundary).

## Floating warnings (the guardrails — carry these into any pull)
- **Detector-implying chains** (signalMix%→lens weight, triggers→lenses, code→route) tempt a
  scoring engine; no detector exists (no AI/backend). Survive ONLY as displayed through-lines
  over existing fixture data, never a computed pipeline.
- **Person-drift guard** (aura / voice-register / scene-role copy seams): any layer that lets
  copy address "you", infer mood/worth, or reframe the *person* trips the master law +
  scan-contract PERSON_TRUTH. Aura especially (no grammar rule) is an open door — anchor to
  visible cues only. **Weight/mythology tell:** if a layer needs a backstory, a self, or a
  fate to make sense, it is floating → KILL.
- **Backlog zones (Vault/Codex)** are not built — note the seam, don't build the destination.
- **Lens enum** is premature until the 7-lens / 10-lens taxonomy is reconciled.

## Latent-link index (raw value — 30 seams, pull-reference)
L1 confidence↔Subject-lock receipt · L2 confidence→tier-band shrinkage · L3 class→hidden-stat
type · L4 mint-trigger→lenses→family · L5 halo material←capture/photo palette · L6
fitMatrix→Fit Coherence stat · L7 signalMix→lens weight→routing · L8 two serials↔Free/Halo
tier · L9 evidence free-gate↔V2 receipt slice · L10 oracle timeline = unassigned 3rd layer ·
L11 pressure metrics↔diagram pressure · L12 objectNo→scanId→card.serial · L13
capture.code→route→class · L14 locale/provenance→Lore Density · L15 Source Record↔left-panel
Source tab (shared provenance chip) · L16 class→Codex · L17 mint record→Vault manifest · L18
12 classes↔24 archetypes (cleanest gap) · L19 smeared gate→single back-seam · L20 voice
register↔family · L21 sealed-stat type↔rarity factor · L22 Reframe levers = trigger deltas
(inverse) · L23 low-info ladder↔scan routing · L24 evidence lens↔closed lens vocab · L25
receiptId→stat (fixture-only) · L26 blocked state→routing copy register · L27
capture.rec=card.minted=batch · L28 aura tokens→trigger cue set · L29 scene-role↔every-outcome
reframe · L30 conditional-stat emission = single cue-gated rule.
*(Most are connective; the person-drift / detector / backlog ones above are the killed/deferred subset.)*

## How to pull
The cleanest standalone keepers are **#1–#5** (all KEEP/High, all connective-only). Order by
the spine: **P1 (class→family map, #2)** is the keystone — author it first as a copy/docs
lookup beside `ARCHETYPE_NOTES`; then **P2 (serial lineage, #1 + #7)** as a Plate 01/06
through-line; then **P3 (#4 + #5)** as validator + spec grounding rules. #3 (route rows) is an
easy standalone win that also satisfies the existing "Route label in Source Record" Ready
item. Each: pre-edit critique → one commit behind `baseline-v1` → verify. Land everything on
the **P4 back-seam**, never as a new floating surface.

# THE MASTER DESIGN SHEET — Fable, over teams 1-4

# THE JUDGEMENT — M1 fleet 3, final pass

Files judged: `_m1-team1.html` (THE HAND), `_m1-team2.html` (THE SPAN — the file, not the "PASSAGE" sheet), `_m1-team3.html` (THE STANDING SENTENCE), `_m1-team4.html` (THE STANDING FIELD). Where sheet and file disagreed, the file was read as truth.

---

## 1. THE MUTUAL-CUT RULE, APPLIED

**No two teams are cut.** The test — one sentence describing both without either description becoming false — fails on every pair, and it fails because of the overtime round, not the first drafts:

- **T2 vs T3** was the live collision (both first-round sheets were "the front door is prose and the products are live words inside it"). Team 2's pivot dissolved it: the built `_m1-team2.html` is a lit rule with marks whose openings are inserted at the touched coordinate. "One printed sentence, no controls" (T3) is false of it; "a measured line that parts where touched" is false of T3, which has zero buttons by declared law.
- **T1 vs T3** share the vertical-serif-flow silhouette, but "time on the page changes what the page offers" (T1: heap, count, earned offer) is false of T3, which has exactly one state forever; "every live element is a word" is false of T1.
- **T1 vs T4** share the lit parchment object with a welded read, but "the field of 222 marks is the subject and the products are acts of gathering from it" is false of T1; "the page performs the product on you and accumulates a record of it" is false of T4, whose `gatherTo` early-returns on repeat by design.
- **T2 vs T4**: ordered measure with a moving opening vs unordered corpus with a fixed seat. No shared sentence survives.

**No solo cut for "the shipped page retuned."** Team 1 is the closest silhouette to what ships and its own sheet admits the exposure — but the heap, the arrival deal, the in-place earned offer, and the absence of columns, tracks and a face-toggle are structural, not tuning. It stands.

All four are therefore quarryable.

---

## 2. THE STRONGEST PART OF EACH

- **TEAM 1 — the commitment point arrives AFTER the visitor has performed the product, with visible residue.** The terms line upgrades in place at the third fall; the heap makes the offer's argument ("those were loose cards, no question asked") out of the visitor's own behaviour. It is the only design in the pool where a visitor's ten minutes leave a trace, and the only commercial mechanism a link list cannot reproduce. Also the only file that names prices (the repo's real ones) and knows a returning visitor.
- **TEAM 2 — the measured axis with named ends.** `given ←→ tonight` states the actual difference between the two products — *when the thing being read was fixed* — as a position, before a word of copy is read. It is the cheapest differentiator in the fleet: two words and two coordinates.
- **TEAM 3 — height is the law, size is not; and the product name is the offer's own text.** The 152px block re-proportions (16×1.6×6 = 153.6) instead of shrinking its contract; type yields on vh so the page never overflows; the anchor's accessible name is the product name; the catchword is the printed grammar for "down continues." The lowest-defect surface in the pool, by enforceable declaration.
- **TEAM 4 — the legend.** Addressing an act lights the systems it is actually made of (birth six of nine, tarot two) and the same call says it aloud for a non-sighted visitor. The product shown as composed of the corpus, held for as long as you look. Also: the session-gated overture — M1 is a station on a track, and only Team 4 priced re-entry.

---

## 3. THE MASTER DESIGN — built and measured

**File: `C:/Users/nilsj/OneDrive/Documents/blue-room-scan-room/_m1-master.html`** (verified live at ten viewports, five-deal cycle exercised — numbers below are measured, not claimed).

**NAME.** THE TWO TENSES.

**THESIS.** The front door's whole argument is one axis — *tonight* against *given* — and the two products are not two of a kind: one is an act you perform and accumulate, the other is a fact that never moves. The offer is earned by the acting, and the still thing is priced beside the pile.

**THE GATE DESCRIPTION** (no banned words). A struck head names the place and its two tenses in one sentence. Beneath it a single measured hairline runs the full measure, its ends named *tonight* and *given*, the run behind tonight lit gold. Two groups hang from points on it. At the tonight end, one warm parchment face has already fallen — the page's light source, its five-line meaning welded to its flank, a control to make it fall again, and beneath that the terms with a live door in them from first paint. Spent faces heap under the live one, keeping their numerals, slewing toward the given end — into the past. At the given end, one small sealed plate that has no transition, no transform, and no class the deal can reach, standing beside The Birth Reading's name, its price, and its six marks as six Codex doors. At the foot, a colophon of routes and a catchword pointing down and out of the screen.

**THE ARRANGEMENT.** One flow, source order = visual order = priority order: head → span → the two tenses (tarot first: free leads) → foot. At ≤900px the span stands on end — one vertical hairline down the flow's start edge, lit above, quiet below, each group keeping its pip. That rotation is the whole responsive story; there is no second layout to break.

**THE CHOICE.** In two places on purpose, because the two continuations are not the same kind of decision. Tarot is accumulated: deal, deal again, and at the third fall the terms sentence upgrades in place to the earned offer; at five the deal retires and the offer stays. Birth is chosen once, available from first paint, and never changes. The visible terms line carries the live `?dev=drawing-room` door from first paint — the swap upgrades the claim, it does not manufacture the exit.

**THE CREAM OBJECT.** Stays, warm, the only light source, at `clamp(200px,38vh,420px)` wide / `clamp(118px,18vh,175px)` narrow — evidence of an event, not a monument. One face only; birth's plate is sealed parchment with a crown, deliberately not a dealable face. One code path writes the surface; the fall is timer-driven, never rAF-gated; the heap grows by transform inside the stack's own box, zero layout.

**WHERE THE 152px LIVES.** Welded to the fallen face's flank, on the same surface, moving with it. 19×1.6×5 = **152.0 measured** at 1440×900. Narrow re-proportions: 16×1.6×6 = **153.6 measured** at 430×900 — height is the law, size is not. Stated deviations: 120px at the ≤720px-height tiers, 112px at the 360-class tier.

**ON THE TRACK.** The only listeners in the file are `click` (deal button, catchword) and `load`. No key handler, no wheel handler, no `INPUT`/`TEXTAREA`, no `[data-scrolls]`, no roving group. `_u1CanGlide` and app.js:4749 are untouched by construction. Chrome-rail clearance `clamp(88px,10vh,108px)` reserved; nothing of ours enters it. On the live page the catchword calls the existing `_u1GlideTo(seat)`.

**AT 430px.** Measured in a real viewport, no `overflow-x` protection anywhere in the document: **430×900 = 900/900 and 430/430, zero elements past either edge, after five deals.** The read sits at the card's flank (the card never thumbnails alone into a text stack), the sealed plate sits beside its door's words, and the heap settles downward at narrow so it cannot slew into the read — a defect the measurement caught (plates were printing 10px over the read's label; now 157 vs 158). Full sweep: 1440×900 → 900/900 · 1280×720 → 720/720 · 1024×700 → 700/700 · 768×1024 → 1024/1024 · 430×900 → 900/900 · 390×844 → 844/844 · 375×667 → 667/667. **Stated floor: 360×640 overflows by 23px** — reported, not chased, rather than butchering the type to hide it.

**AT SCALE.** M1 stays the two-tense front door permanently, and the axis makes that a claim: a third product has no honest coordinate between *given* and *tonight* without becoming a tile row, which app.js:2453-2459 rejects by name. Rooms three through twelve live behind ROOMS in the rail; the corridor takes the growth pressure; the catchword takes the vertical universe. What grows free is the deck — one array, one `write()`.

**WHAT IT FIXES** (critique items). #1 made unauthorable: no fixed inline sizes, no overflow concealment, zero-offender sweeps at eight sizes. #3: one write path, first paint through the same call. #4: one `:focus-visible` ring on every control. #5: the birth door is a real `<a>` whose accessible name is its own visible text. #6: the fallen card names itself in the polite live region armed after arrival, written by the same call as the meaning. #12: the price sits where the unit was dropped ("the Deep Read, $2.99"). #23/teardown: a visible downward mark exists as composition. Duplicate destinations deleted, not repaired: one route to each room, one Codex line in the colophon (carrying T4's extent claim: "222 marks, nine systems"). Plus the two things all four teams initially missed: **prices** (the repo's own — $1.99/$2.99 at drawing-room.js:82/85, $4.99/$7.99 at app.js:1476, written in markup so no-JS arrivals get them) and **the return** (read-only, fail-open script on `br_dr_sitting_used` / `br_has_reading` / `br_holdings`: a spent visitor's terms stop promising a free Sitting; a holder's birth door stops selling what is owned and names the second name).

**WHAT IT COSTS.** Birth has no motion and a small mass — the sealed plate is the mitigation, and if the given end reads cold in the builder's first screenshot, that failure is visible in one look. The free demonstration is a real commercial risk; the cap at five and the offer-converts-to-intake are the mitigations. The 152px reserves its height whether or not an entry fills it. The 360×640 floor is real. And the axis rests on two italic words — if *tonight/given* fails to read as a measure, the hairline is decoration, and that too is visible in one screenshot.

**WHAT I TOOK FROM WHOM.** T1: the arrival deal, the heap with numerals, the in-place earned offer and cap, one-code-path write, the height-query discipline, prices, the return script, timer-not-rAF boot. T2: the named-end axis, its rotation at narrow, the pip grammar. T3: height-is-the-law re-proportioning, the colophon + catchword foot, `margin-top:auto` foot matter, the premise-first head sentence. T4: the extent line as the Codex route, the parchment-as-light-source law.

**WHAT I REFUSED AND WHY.** T2's homeless opening and one-at-a-time state — a front door with both products permanently visible needs no opening mechanic, and exactly-one-visible re-creates the face-toggle's problem in better clothes. T3's zero-object law — in a category where the product is an object, refusing to show one is a bet the critique itself calls unsettleable, so I did not make it. T4's 222-mark field and travel animation — the most beautiful thing in the pool and the most expensive; its claim survives here as one colophon line at ~2% of the cost, and a field that must thin to 38% on phones is not a phone design. T1's slant — the one line item in that file an adjudicator could call "the shipped page, moved"; the heap supplies the only diagonal this page needs. And every RULED OUT item stands: no priced door tiles (the price is a clause inside each product's one existing sentence, not a badge or a row), no `M2_LAST` persistence (the deal count is session-only, the return bits are the rooms' own), no four-band stack (the two tenses hang from an axis, and the phone arrangement is the same law rotated, not a rewrite).
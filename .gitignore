Thumbs.db
desktop.ini

# BR-S111: SRC-03/04/05 prototype photos are now PUBLISHED for the public web
# deploy (GitHub Pages) — owner authorized public faces 2026-06-23. Previously
# gitignored as personal/local-only; that hold is lifted by explicit consent.
.claude/

# BR-S355: the public gate's identity denylist. Holds the real names, the email and the
# home town as literal strings — the exact data it exists to keep off the public site, so
# it is never committed. gate_public.py FAILS rather than skips when it is absent.
gate_identity.txt
dist/
